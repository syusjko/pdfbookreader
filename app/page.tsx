import Player from '../components/Player';

export default function Home() {
  return (
    <main className="fixed inset-0 w-full h-[100dvh] bg-white font-sans text-black overflow-hidden">
      <Player />
    </main>
  );
}
